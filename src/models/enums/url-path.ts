export enum UrlPath {
  HOME = '/',
  SIGN_IN = 'sign-in',
  REGISTRATION = 'registration',
  CATALOG = 'catalog',
  ABOUT = 'about',
  USER_PROFILE = 'user-profile',
  BASKET = 'basket',
  NOT_FOUND = '*',
}
